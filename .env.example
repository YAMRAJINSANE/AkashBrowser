# Environment Configuration
# Copy this to .env and update with your values

# Chrome executable path (auto-detected if not set)
CHROME_PATH=

# Enable debug mode
DEBUG=false

# App data directory (defaults to user AppData)
APP_DATA_DIR=

# Log level (error, warn, info, debug)
LOG_LEVEL=info
